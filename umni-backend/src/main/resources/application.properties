server.port = 8082

spring.application.name=core-service
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/

spring.data.mongodb.uri=${MONGODB_URI}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:5173,http://127.0.0.1:5173}



# this is 64 string here im using the HS256/HS512 encryption 
jwt.secret=${JWT_SECRET}


# if key is very short it will throw the weakkeyexception

jwt.expiration.ms=86400000    

spring.data.mongodb.connection-pool.max-size=20
spring.data.mongodb.connection-pool.min-size=5
spring.data.mongodb.connection-pool.max-wait-time.ms=2000

server.http2.enabled=true

deepseek.api.key = ${DEEPSEEK_API_KEY}
deepseek.api.url = https://api.deepseek.com/v1/chat/completions
deepseek.model = deepseek-v4-flash

gemini.api.key=${GEMINI_API_KEY}
gemini.model=gemini-2.5-flash-preview

aws.s3.bucket=${AWS_S3_BUCKET:umni-vision-images}
aws.s3.region=${AWS_REGION:ap-south-1}

aws.s3.access-key=${AWS_ACCESS_KEY}
aws.s3.secret-key=${AWS_SECRET_KEY}

note.service.url=${NOTE_SERVICE_URL:http://localhost:8083}

# Allow AI requests up to 2 minutes
spring.mvc.async.request-timeout=120000

# OpenAI image generation (gpt-image-1-mini)
openai.api.key=${OPENAI_API_KEY}
openai.image.model=gpt-image-1-mini
openai.image.quality=low
openai.image.size=1024x1024
